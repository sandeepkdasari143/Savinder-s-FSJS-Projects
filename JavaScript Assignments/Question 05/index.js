//05. Declare a varibale and assign string value to it and then split it into an array using split() method

const str = "JavaScript is easy to learn. Even a child can also master it 😅😂"
const arr = str.split(" ");  //We are splitting the string based on whitespace

//output
console.log(arr);